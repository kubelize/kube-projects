apiVersion: v1
kind: Service
metadata:
  name: linkstack-service
spec:
  selector:
    app: linkstack
  ports:
    - port: 80
      targetPort: 80
    - port: 443
      targetPort: 443
  type: ClusterIP