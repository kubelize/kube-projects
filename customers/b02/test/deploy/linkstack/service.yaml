apiVersion: v1
kind: Service
metadata:
  name: linkstack-service
spec:
  selector:
    app: linkstack
  ports:
    - port: 8080
      targetPort: 80
    - port: 8443
      targetPort: 443
  type: ClusterIP