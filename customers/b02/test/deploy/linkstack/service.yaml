apiVersion: v1
kind: Service
metadata:
  name: linkstack
spec:
  selector:
    app: linkstack
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: https
      port: 8443
      targetPort: https
  type: ClusterIP
