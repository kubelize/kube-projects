apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: test-nginx
  namespace: flux-reserved
spec:
  interval: 30s
  url: https://github.com/kubelize/kube-projects
  ref:
    branch: dev